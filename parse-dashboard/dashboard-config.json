{
    "apps": [
        {
            "serverURL": "http://{HOST}:{PORT}/parse",
            "appId": "{APP_ID}",
            "masterKey": "{MASTER_KEY}",
            "appName": "{APP_NAME}",
            "iconName": "{APP_ICON}"
        }
    ],
    "users": [
    ],
    "useEncryptedPasswords": false,
    "iconsFolder": "icons",
    "trustProxy": 1
}
